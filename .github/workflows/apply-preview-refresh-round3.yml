name: Apply preview refresh cadence hardening

on:
  push:
    branches:
      - hardening/live-lock-v35
    paths:
      - "scripts/harden-preview-refresh-round3.py"
      - ".github/workflows/apply-preview-refresh-round3.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apply-preview-refresh-round3
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hardening/live-lock-v35
          fetch-depth: 0
      - name: Apply deterministic transform
        run: python scripts/harden-preview-refresh-round3.py
      - name: Commit preview refresh cadence hardening
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/v1/completed-worker-live-lock.ts
          git diff --cached --quiet && { echo 'NO_CHANGES'; exit 0; }
          git commit -m 'Throttle early previews while preserving T-25 freshness'
          git push origin HEAD:hardening/live-lock-v35
