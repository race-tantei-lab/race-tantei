name: Research ML discovery demanded official odds (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Deprecated. Use research-ml-discovery-demanded-odds-v2.yml.'
