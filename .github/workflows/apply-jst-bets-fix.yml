# trigger PR hotfix runner
name: Apply JST and bets fix

on:
  push:
    branches: [fix-jst-dates-and-bets]

permissions:
  contents: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR hotfix runner handles this branch"
