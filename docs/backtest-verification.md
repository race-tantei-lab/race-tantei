# August 1 retrospective verification

This branch exists only to run CI against the production backtest and race-name repair changes already present on main.

Re-run after serializing D1 writes, switching race-name repair to official result pages, and supporting `titleRaceNameNormal`.
