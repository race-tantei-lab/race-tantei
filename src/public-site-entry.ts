import legacy from "./learning-entry.js";
import type { Env } from "./v1/types.js";
import { ensurePublicSchema,getPublicMetrics,getPublicMonthlyMetrics,getRaceDates,getRaces,getRaceDetail,getRuleCatalog,type PublicMetric } from "./v1/public-site-db.js";
import { escapeHtml } from "./v1/utils.js";

const STYLE=`:root{color-scheme:dark;--bg:#07111b;--panel:#0f1c29;--line:#294158;--text:#f4f8fb;--muted:#9cb0c4;--green:#52d5a4;--gold:#f0cf79;--red:#ff8580}*{box-sizing:border-box}body{margin:0;background:linear-gradient(180deg,#07111b,#091522 55%,#07111b);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Noto Sans JP",sans-serif}a{color:inherit;text-decoration:none}.wrap{max-width:1050px;margin:auto;padding:18px 14px 52px}.top{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:18px;position:sticky;top:0;background:rgba(7,17,27,.92);backdrop-filter:blur(12px);z-index:10;padding:10px 0}.brand{font-size:27px;font-weight:950;color:var(--green)}nav{display:flex;gap:7px;overflow:auto}nav a{white-space:nowrap;padding:8px 11px;border:1px solid var(--line);border-radius:999px;font-size:13px}.hero,.metric,.panel,.race,.ticket,.runner,.note{border:1px solid var(--line);background:var(--panel);border-radius:18px}.hero{padding:19px}.hero h1{margin:0 0 8px;font-size:26px}.hero p{margin:5px 0;color:var(--muted);line-height:1.7}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:12px 0}.metric{padding:15px}.metric b,.metric small{display:block}.metric strong{display:block;font-size:31px;margin:5px 0;color:var(--green)}.metric small{color:var(--muted)}.section{margin-top:24px}.section h2{margin:0 0 10px}.picker{display:flex;gap:7px;overflow:auto;padding:3px 0 9px}.picker a{white-space:nowrap;border:1px solid var(--line);border-radius:999px;padding:9px 12px;background:var(--panel)}.picker a.active{border-color:var(--green);background:#12352d}.picker a.today{color:#052019;background:var(--green);border-color:var(--green);font-weight:900}.panel{padding:14px}.race-grid{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px}.race{padding:12px;min-height:105px}.race b,.race span{display:block}.race b{font-size:19px}.race span{font-size:12px;color:var(--muted);margin-top:4px}.race.selected{border-color:#2d8c70;background:#0f2d27}.race.selected .state{color:var(--green)}.race.skip{opacity:.7}.race.pending{border-style:dashed}.state{margin-top:8px!important;font-weight:800;color:var(--gold)!important}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:14px}table{width:100%;border-collapse:collapse;min-width:650px}th,td{padding:10px;border-bottom:1px solid var(--line);text-align:left}.muted{color:var(--muted)}.ticket-grid{display:grid;gap:9px}.ticket{padding:13px}.ticket-head{display:flex;justify-content:space-between;gap:10px}.stakes{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin-top:9px}.stakes div{background:#0a1621;border-radius:10px;padding:8px}.reason{font-size:12px;color:var(--muted);line-height:1.6;margin-top:8px}.runner-list{display:grid;gap:7px}.runner{padding:11px;display:grid;grid-template-columns:48px 1.3fr 1fr 1fr 70px;gap:8px;align-items:center}.runner .no{font-size:22px;font-weight:900}.condition-group{margin:10px 0}.condition-group summary{cursor:pointer;font-weight:900}.condition-list{padding-left:20px;color:var(--muted);line-height:1.65}.note{padding:14px;line-height:1.75;color:var(--muted)}.hit{color:var(--green);font-weight:900}.miss{color:var(--red)}@media(max-width:760px){.metrics{grid-template-columns:1fr}.race-grid{grid-template-columns:repeat(3,minmax(0,1fr))}.runner{grid-template-columns:40px 1fr 1fr}.runner .extra{display:none}.stakes{grid-template-columns:1fr}.top{align-items:flex-start}.brand{font-size:23px}}`;
function shell(title:string,body:string){return `<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#07111b"><title>${escapeHtml(title)}｜レース探偵</title><style>${STYLE}</style></head><body><main class="wrap"><header class="top"><a class="brand" href="/">レース探偵</a><nav><a href="/conditions">条件詳細</a><a href="/guide">レース探偵の見方</a></nav></header>${body}</main></body></html>`}
function html(body:string,status=200){return new Response(body,{status,headers:{"content-type":"text/html; charset=utf-8","cache-control":"no-store, max-age=0","x-race-public-ui":"1"}})}
function yen(v:number){return `${Math.round(v).toLocaleString('ja-JP')}円`}
function metricCards(rows:PublicMetric[]){return `<section class="metrics">${rows.map(r=>`<a class="metric" href="/performance#monthly"><b>${r.course}</b><strong>${r.roiPct===null?'—':r.roiPct.toFixed(1)+'%'}</strong><small>${yen(r.stakeYen)} → ${yen(r.returnYen)}</small></a>`).join('')}</section>`}
function dateLabel(d:string){const [y,m,da]=d.split('-').map(Number);const wd=['日','月','火','水','木','金','土'][new Date(Date.UTC(y,m-1,da)).getUTCDay()];return `${y}年${m}月${da}日（${wd}）`}
function jstToday(){return new Intl.DateTimeFormat('sv-SE',{timeZone:'Asia/Tokyo',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())}
function timeOnly(v:string|null){return v?.match(/\d{1,2}:\d{2}/)?.[0]??'時刻未定'}
function validDate(v:string|null){return v&&/^\d{4}-\d{2}-\d{2}$/.test(v)?v:null}

async function home(db:D1Database,url:URL){
 const [metrics,dates]=await Promise.all([getPublicMetrics(db),getRaceDates(db)]); if(!dates.length)return shell('レース一覧',`<section class="hero"><h1>レース探偵</h1><p>レース情報を取得中です。</p></section>`);
 const today=jstToday(),available=dates.map(x=>x.raceDate); let date=validDate(url.searchParams.get('date')); if(!date||!available.includes(date))date=available.includes(today)?today:available[0];
 const years=[...new Set(available.map(d=>d.slice(0,4)))]; let year=url.searchParams.get('year')||date.slice(0,4);if(!years.includes(year))year=date.slice(0,4); const months=[...new Set(available.filter(d=>d.startsWith(year+'-')).map(d=>d.slice(0,7)))];let month=url.searchParams.get('month')||date.slice(0,7);if(!months.includes(month))month=months[0];const monthDates=dates.filter(x=>x.raceDate.startsWith(month));if(!monthDates.some(x=>x.raceDate===date))date=monthDates[0].raceDate;const info=dates.find(x=>x.raceDate===date)!;let venue=url.searchParams.get('venue')||info.venues[0];if(!info.venues.includes(venue))venue=info.venues[0];const races=await getRaces(db,date,venue);
 const href=(d:string,v:string)=>`/?year=${d.slice(0,4)}&month=${d.slice(0,7)}&date=${d}&venue=${encodeURIComponent(v)}`;
 const yearNav=years.map(y=>{const d=dates.find(x=>x.raceDate.startsWith(y+'-'))!;return `<a class="${y===year?'active':''}" href="${href(d.raceDate,d.venues[0])}">${y}年</a>`}).join('');
 const monthNav=months.map(m=>{const d=dates.find(x=>x.raceDate.startsWith(m))!;return `<a class="${m===month?'active':''}" href="${href(d.raceDate,d.venues[0])}">${Number(m.slice(5))}月</a>`}).join('');
 const dayNav=monthDates.map(x=>`<a class="${x.raceDate===date?'active':''} ${x.raceDate===today?'today':''}" href="${href(x.raceDate,x.venues[0])}">${x.raceDate===today?'今日 ':''}${Number(x.raceDate.slice(8))}日</a>`).join('');
 const venueNav=info.venues.map(v=>`<a class="${v===venue?'active':''}" href="${href(date,v)}">${escapeHtml(v)}</a>`).join('');
 const cards=races.map(r=>{const state=r.selected?'買い目公開':r.frozen?'買い目なし':'選定中・公開は発走15分前まで';return `<a class="race ${r.selected?'selected':r.frozen?'skip':'pending'}" href="/races/${encodeURIComponent(r.raceId)}"><b>${r.raceNo}R</b><span>${timeOnly(r.startTimeJst)}</span><span>${escapeHtml(r.raceName)}</span><span class="state">${state}</span></a>`}).join('');
 return shell('レース一覧',`<section class="hero"><h1>${date===today?'今日のレース':'レース一覧'}</h1><p>全開催を同じルールで表示しています。公開した買い目は、その後の学習やオッズ変化で書き換わりません。</p></section>${metricCards(metrics)}<section class="section"><h2>開催を選ぶ</h2><div class="picker">${yearNav}</div><div class="picker">${monthNav}</div><div class="picker">${dayNav}</div><div class="picker">${venueNav}</div></section><section class="panel"><h2>${escapeHtml(dateLabel(date))}・${escapeHtml(venue)}</h2><div class="race-grid">${cards}</div></section>`);
}
async function performance(db:D1Database){const [metrics,months]=await Promise.all([getPublicMetrics(db),getPublicMonthlyMetrics(db)]);return shell('回収率',`<section class="hero"><h1>累計回収率</h1><p>公開済みの買い目を固定したまま積み上げています。</p></section>${metricCards(metrics)}<section class="section" id="monthly"><h2>月ごとの回収率</h2><div class="table-wrap"><table><thead><tr><th>月</th><th>コース</th><th>購入</th><th>払戻</th><th>回収率</th></tr></thead><tbody>${months.map(x=>`<tr><td>${escapeHtml(x.month.replace('-','年'))}月</td><td>${x.course}</td><td>${yen(x.stakeYen)}</td><td>${yen(x.returnYen)}</td><td>${x.roiPct===null?'—':x.roiPct.toFixed(1)+'%'}</td></tr>`).join('')}</tbody></table></div></section>`)}
async function conditions(db:D1Database){const rules=await getRuleCatalog(db);const groups=[...new Set(rules.filter(r=>r.betType!=='共通').map(r=>r.betType))];return shell('条件詳細',`<section class="hero"><h1>買い目の条件</h1><p>すべてのレースを同じ考え方で判定し、結果を見てから買い目を変えることはありません。</p></section><section class="note"><b>共通ルール</b><br>・各会場で条件が強い5レースを買い目表示対象にします。<br>・1レース3〜10点。1点に使う金額はレース予算の35%までです。<br>・6券種はそれぞれ別々の条件で判定します。<br>・極端な高オッズほど購入比率を抑えます。<br>・会場、芝/ダート、距離、頭数、R番号、馬場、天候、人気構成、オッズ帯、市場の偏り、馬・騎手・厩舎の過去成績などを組み合わせます。<br>・履歴はそのレースより前に確定している情報だけを使います。<br>・公開した買い目は後から変更しません。</section><section class="section"><h2>採用している条件一覧</h2><p class="muted">券種ごとに開くと現在の条件をすべて確認できます。</p>${groups.map(g=>`<details class="condition-group"><summary>${escapeHtml(g)}（${rules.filter(r=>r.betType===g).length}条件）</summary><ol class="condition-list">${rules.filter(r=>r.betType===g).map(r=>`<li>${escapeHtml(r.conditionText)}</li>`).join('')}</ol></details>`).join('')}</section>`)}
function guide(){return shell('レース探偵の見方',`<section class="hero"><h1>レース探偵の見方</h1><p>初めて見る人向けに、表示の意味と更新タイミングをまとめています。</p></section><section class="note"><b>買い目の公開</b><br>出走情報と公式オッズがそろい次第判定し、対象レースは<b>遅くとも発走15分前まで</b>に固定します。条件がそろえばそれより早く公開します。公開後の買い目・金額は変更しません。<br><br><b>レース情報の更新</b><br>開催日、出走馬、オッズ、結果、払戻は公式情報を定期取得します。結果確定後は公開済み買い目に払戻だけを反映します。<br><br><b>表示の意味</b><br>各会場で条件が強い5レースが買い目対象です。「買い目公開」は購入内容を確認できます。「買い目なし」は対象外。「選定中」は公式情報待ちです。<br><br><b>3つのコース</b><br>ライト2,000円、スタンダード5,000円、プレミアム10,000円。選ぶ馬券は共通で金額配分だけが異なります。<br><br><b>学習</b><br>新しく確定した情報は将来の条件改善に利用します。改善は次に公開する買い目から反映し、過去の買い目や成績を書き換えません。</section>`)}
async function racePage(db:D1Database,id:string){const {race,tickets,runners}=await getRaceDetail(db,id);if(!race)return shell('レース',`<section class="hero"><h1>レースが見つかりません</h1></section>`);const state=race.selected?(tickets.length?'買い目公開済み':'買い目公開予定'):(race.frozen?'このレースは買い目なし':'選定中・遅くとも発走15分前までに確定');const ticketHtml=tickets.length?`<div class="ticket-grid">${tickets.map(t=>{const hit=(t.payoutPer100??0)>0;return `<article class="ticket"><div class="ticket-head"><div><b>${escapeHtml(t.betType)} ${escapeHtml(t.combination)}</b><span class="muted">${t.officialOdds===null?'':`固定時 ${t.officialOdds}倍`}</span></div>${t.payoutPer100===null?'':`<strong class="${hit?'hit':'miss'}">${hit?`100円→${yen(t.payoutPer100)}`:'不的中'}</strong>`}</div><div class="stakes"><div>ライト<br><b>${yen(t.lightStake)}</b></div><div>スタンダード<br><b>${yen(t.standardStake)}</b></div><div>プレミアム<br><b>${yen(t.premiumStake)}</b></div></div><div class="reason">条件：${escapeHtml(t.conditionText)}</div></article>`}).join('')}</div>`:`<div class="note">${escapeHtml(state)}</div>`;const runnerHtml=runners.map(r=>`<div class="runner"><div class="no">${r.horseNo}</div><div><b>${escapeHtml(r.horseName)}</b><br><span class="muted">${escapeHtml(r.sexAge??'')} ${escapeHtml(r.jockey??'')}</span></div><div>${r.popularity?`${r.popularity}番人気`:''}<br>${r.winOdds?`${r.winOdds}倍`:''}</div><div class="extra">${escapeHtml(r.trainer??'')}</div><div class="extra">${r.finishPosition?`${r.finishPosition}着`:''}</div></div>`).join('');return shell(`${race.venue}${race.raceNo}R`,`<section class="hero"><h1>${escapeHtml(race.venue)} ${race.raceNo}R　${escapeHtml(race.raceName)}</h1><p>${escapeHtml(race.raceDate)}　${timeOnly(race.startTimeJst)}　${escapeHtml(race.surface??'')} ${race.distanceM??''}m ${escapeHtml(race.trackCondition??'')}</p><p><b>${escapeHtml(state)}</b></p></section><section class="section"><h2>買い目</h2>${ticketHtml}</section><section class="section"><h2>出走馬</h2><div class="runner-list">${runnerHtml}</div></section>`)}

export default {async fetch(request:Request,env:Env,ctx:ExecutionContext):Promise<Response>{const url=new URL(request.url);if(request.method==='GET'){try{await ensurePublicSchema(env.DB)}catch{}if(url.pathname==='/')return html(await home(env.DB,url));if(url.pathname==='/performance')return html(await performance(env.DB));if(url.pathname==='/conditions')return html(await conditions(env.DB));if(url.pathname==='/guide')return html(guide());if(url.pathname.startsWith('/races/'))return html(await racePage(env.DB,decodeURIComponent(url.pathname.slice(7))));}if(legacy.fetch)return legacy.fetch(request,env,ctx);return new Response('NOT_FOUND',{status:404});},async scheduled(controller:ScheduledController,env:Env,ctx:ExecutionContext){if(legacy.scheduled)await legacy.scheduled(controller,env,ctx)}} satisfies ExportedHandler<Env>;
